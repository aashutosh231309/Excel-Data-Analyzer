/**
 * PostCSS pipeline for Tailwind CSS.
 * The project is CommonJS by default, so this file uses CommonJS syntax.
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
